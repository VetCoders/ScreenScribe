# Screenscribe: providers (gpt-transcribe · wss-libraxis · xAI STT/TTS), OAuth port, WebKit tafla spec

- Repo: `vetcoders/screenscribe` @ `9bd7b2b` (main), 2026-09-10
- Author: claude (fork of codescribe session `canary-gate-approval`)
- Founder ask (2026-09-10, verbatim intent): "ogarnij obsługę gpt-transcribe, wss-libraxis, grok api stt tts w screenscribe skutecznie do końca … zaportuj oauth openai i xai jako alternatywną metodę auth … zaplanuj webkit-based taflę jako hosta dla native-like screenscribe app wysokopoziomowo, na razie tylko ze specs portu z aktualnego tipa HEAD brancha vibecrafted z dragona (folder vibecrafted-app)".

## Current truth (verified 2026-09-10)

- STT path: `screenscribe/transcribe.py::_request_transcription_payload` — multipart `file`, `model`, `language`, `response_format`; `verbose_json` → `json` fallback on 400 landed in `9bd7b2b`. Installed CLI (`~/.local/bin/screenscribe` 0.1.19) predates it: the 13:17 review died at chunk 1/12 with that 400.
- Config: `screenscribe/config.py` — `ScreenScribeConfig` with `provider` (`libraxis|openai|custom`), per-endpoint keys, `stt_fallback_*`. No xAI, no TTS, no websocket, no OAuth anywhere (literal census = 0 hits).
- Codescribe sources to port (read-only reference, `~/vc-workspace/vetcoders/codescribe`):
  - OAuth: `core/llm/account_auth/{mod,device_code,server,pkce,paste_code}.rs`. OpenAI = Codex device flow (`POST {issuer}/api/accounts/deviceauth/usercode` → poll `/deviceauth/token` (403/404 = pending) → PKCE exchange `POST {issuer}/oauth/token` with `redirect_uri={issuer}/deviceauth/callback`; issuer `https://auth.openai.com`, public client `app_EMoamEEZ73f0CkXaXp7hrann`, scope `openid profile email offline_access`). xAI = RFC 8628 (`POST https://auth.x.ai/oauth2/device/code` form `client_id, scope, referrer`; poll `POST /oauth2/token` grant `urn:ietf:params:oauth:grant-type:device_code`; errors `authorization_pending|slow_down|access_denied|expired_token`; client `b1a00492-073a-47ea-816f-4c329264a828`; scope `openid profile email offline_access grok-cli:access api:access`).
  - **Known limit (2026-09-08 codescribe walk-around):** an OpenAI ChatGPT account token is NOT accepted by `api.openai.com` REST (no Codex route) → for Screenscribe the OpenAI OAuth is identity/status only until a Codex-backend route exists. xAI account token carries `api:access` and is usable on `api.x.ai`.
  - Libraxis live STT wire (`core/asr_session/cloud.rs`): `wss://api.libraxis.cloud/v1/audio/transcribe`, `Authorization: Bearer`, first text frame `{"type":"session.start","protocol_version":1,"session_id","locale","vocabulary","audio":{"encoding":"pcm_s16le","sample_rate_hz","channels":1,"frame_header":"sequence_u64_be"}}`, binary frames = 8-byte big-endian sequence + PCM16LE; server frames `hello|ack|ready|vad.sample|speech.start|speech.end` (ignore), `transcript.partial|transcript` (text), `transcript.final`, `error{code}`, `end|session.ended|stream.closed`.
  - xAI wire adapter (`adapt_xai`): `transcript.created` (ignore), `transcript.partial{text,is_final,speech_final,start,duration}`, `transcript.done`, `error`.
- xAI official docs (fetched 2026-09-10, docs.x.ai): `POST https://api.x.ai/v1/stt` multipart `file|url`, opt `language, format, diarize, keyterm*, vad_threshold, audio_format, sample_rate`; response `{text, language, duration, words:[{text,start,end,speaker?}]}`; `wss://api.x.ai/v1/stt?sample_rate&encoding=pcm|mulaw|alaw|opus&interim_results&endpointing&language`; client binary frames + `{"type":"finalize"}` + `{"type":"audio.done"}`. `POST https://api.x.ai/v1/tts` JSON `{text (≤15k), voice_id (default eve), language (required), output_format:{codec mp3|wav|pcm|mulaw|alaw, sample_rate, bit_rate}, speed 0.7–1.5, with_timestamps}` → raw audio bytes (or JSON envelope with base64 when timestamps). `GET /v1/tts/voices`. `wss://api.x.ai/v1/tts` streaming variant.
- Dragon `vibecrafted-app` @ `449af692` (branch `feat/dmg-first-run`, clean): Rust workspace (`mux-agent`=rmcp-mux, `tui-agent`=voc, `tray-agent`, `shell-agent/ffi` UniFFI + `shell-agent/app` xcodegen AppKit app: `AppDelegate`, `MainWindowController`, `MainSplitViewController`, `Sidebar/Canvas/Inspector/MissionControlViewController`; runtime via `/tmp/vibecrafted-mux.sock`; first-run decisions in `~/.config/vibecrafted/config.toml`). No WebKit anywhere. `project.yml` = deployment 14.0, Swift 6, prebuild cargo+uniffi, embedded dylib, postbuild embeds Rust binaries.

## Cuts

| Lane | Deliverable | Gate |
|---|---|---|
| A providers | `screenscribe/providers/` (xai STT REST → `TranscriptionResult` with real word timing; xai TTS REST + voices; STT model capability table so `gpt-transcribe*`/`gpt-4o-transcribe*` go straight to `json`, whisper keeps `verbose_json`); `screenscribe/stt_stream.py` (one `StreamingSttClient` interface, `LibraxisStreamClient` + `XaiStreamClient`, sync generator over `websockets`); config: `provider=xai` preset, `tts_*` fields, `stt_live_endpoint`; CLI: `screenscribe tts`, `screenscribe transcribe --live`, `--stt-provider`; `make install` | `make check`, `make test` green; RED-first tests with recorded fixtures; no network in unit tests |
| B oauth | `screenscribe/account_auth/` port (device-code OpenAI + xAI, PKCE, token store `~/.config/screenscribe/accounts.json` 0600, refresh, status); `screenscribe auth login|status|logout <provider>`; bearer resolution order: explicit key → account token (xai usable, openai identity-only with explicit warning) | same gates; token never printed; tests with `respx`/httpx mock |
| C tafla spec | `docs/plans/SS_TAFLA_WEBKIT_HOST_SPEC.md`: high-level architecture of a WebKit-hosted native-like Screenscribe app, port spec from dragon `vibecrafted-app` HEAD (what to reuse: xcodegen `project.yml`, UniFFI bridge shape, window/split controllers, install moment; what changes: `WKWebView` canvas hosting the existing `review_server`/`analyze_server` on loopback with per-launch token, `WKScriptMessageHandler` bridge contract, file drop, mic, keychain auth handoff to lane B, notifications, packaging/DMG/notarize reuse) | doc only, no code |

## Non-goals (this pass)
- No Speech-to-Speech / realtime voice agent (`/v1/realtime`).
- No OpenAI TTS (not asked; leave hook in provider table).
- No keyring dependency (file store first; keychain in tafla host later).

## Lane B report — OAuth port (claude, 2026-09-10)

**Landed (uncommitted, working tree):**
- `screenscribe/account_auth/__init__.py` — registry (`ProviderOAuthConfig` rows `openai`/`xai`; issuer env `SCREENSCRIBE_{OPENAI,XAI}_OAUTH_ISSUER`, client id env `SCREENSCRIBE_{OPENAI,XAI}_OAUTH_CLIENT_ID`, shipped public defaults), token store `~/.config/screenscribe/accounts.json` (dir 0700, file 0600, atomic replace), `AccountTokens` (repr never shows a token), `account_status()` (email from unverified id_token payload, display only), `refresh_tokens()` (form `grant_type=refresh_token`, carries un-rotated refresh token + id_token forward), `resolve_bearer()` (explicit key → account; `openai` returns `identity_only=True` + warning citing the 2026-09-08 codescribe finding).
- `screenscribe/account_auth/pkce.py` — S256 verifier/challenge (RFC 7636 vector pinned).
- `screenscribe/account_auth/device_code.py` — OpenAI Codex flow (JSON usercode → poll 403/404 = pending → form code exchange with `redirect_uri={issuer}/deviceauth/callback`, `code_verifier` from the poll body) and xAI RFC 8628 (form `client_id, scope, referrer=screenscribe`; poll grant `urn:ietf:params:oauth:grant-type:device_code`; `authorization_pending`/`slow_down` (+5 s)/`access_denied`/`expired_token`; interval floor 5 s; 15 min budget). `client`/`sleep` injectable.
- `screenscribe/cli_auth.py` + 2 lines in `cli.py` — `screenscribe auth login <openai|xai> [--no-browser]`, `auth status`, `auth logout <provider>`.
- `screenscribe/config.py` — `get_stt_api_key/get_llm_api_key/get_vision_api_key` fall back to the account bearer only when the endpoint host is `api.x.ai`; `api.openai.com` emits a one-time `UserWarning` and still returns the empty key (API key required). Explicit keys short-circuit before any file read; libraxis/custom hosts never touch the store. `validate()` untouched. Lane A's `get_tts_api_key()` (landed during this lane) returns `tts_api_key or get_stt_api_key()`, so the account fallback already reaches TTS — gated on the STT endpoint host, not the TTS one; wrap it directly if the two hosts ever diverge.
- `NOTICE` (new) — discloses both public client ids + overrides. `USAGE.md` — `screenscribe auth` section + ToC entry.

**Tests:** `tests/test_account_auth.py` (25) + `tests/test_cli_auth.py` (4), all `httpx.MockTransport`, `Path.home` → tmp, no network/no browser: happy path both providers, OpenAI pending(403,404)→success + exact exchange body, xAI pending→slow_down→success with sleeps `[5,10,10]`, denied/expired/unknown error, timeout, missing fields, refresh (form body + carry-forward), 0600/0700 modes, per-provider slots + logout, explicit-key precedence without touching the store, openai identity-only warning (once, token never in message), config host gating, CLI output never contains tokens.

**Gates (tree-wide, 2026-09-10):** ruff clean; bandit clean; mypy — 1 error, `screenscribe/stt_stream.py:389` (Lane A file, not mine); pytest 1336 passed / 6 failed — the 6 are `test_review_app_*` node-harness tests that pass on a clean `HEAD` worktree and fail here only because `/Users/maciejgad/vc-workspace/package.json` (stray `unicode-puzzles-mcp` manifest, `"type": "module"`) makes Node 26 load `tests/_review_dom.js` as ESM. Environment, outside the repo, pre-existing; fix = remove/rename that file or add `tests/package.json` with `{"type":"commonjs"}` (not done — outside lane scope).

**Verified only against mocks (no live sign-in was performed):** every endpoint path, body shape and error code above is taken from the Rust reference and the plan; the real `auth.openai.com` / `auth.x.ai` behaviour (interval values, `verification_uri_complete`, refresh-token rotation, whether xAI returns `id_token`) is unverified. Deviations from the Rust port: missing/0 poll interval defaults to 5 s (Rust would spin at 0); xAI floor is 5 s (Rust: 1 s); no single-flight refresh lock (CLI is single-process).

**Open questions:** (1) should `screenscribe auth login openai` be offered at all while it is identity-only, or hidden behind a flag? (2) Where does `provider=xai` preset (Lane A) declare its hosts — `_ACCOUNT_HOSTS` in config.py currently hardcodes `api.x.ai`; move it onto `ProviderOAuthConfig.api_hosts` once Lane A's preset exists. (3) Keychain storage deferred to the tafla host per plan non-goals.

## Lane A report — providers (claude, 2026-09-10)

**Landed (uncommitted, working tree; coordinator commits):**
- `screenscribe/transcribe.py` — `STT_JSON_ONLY_MODEL_PREFIXES` + `preferred_stt_response_format(model)`: `gpt-transcribe*` / `gpt-4o-transcribe*` / `gpt-4o-mini-transcribe*` request `json` on the first call (no wasted 400); whisper-family and unknown models keep `verbose_json` with the `9bd7b2b` 400 fallback. `_is_xai_endpoint()` + `_transcribe_via_xai()`: `transcribe_audio` / `transcribe_audio_bytes` route to `providers.xai` when the STT host is `api.x.ai` (no `model`, no `response_format`, `language` optional); `transcribe_audio_chunked` inherits it per chunk.
- `screenscribe/providers/{__init__,xai}.py` (new) — `transcribe_file_xai()` (multipart `file` last, optional `language`, `diarize`, repeatable `keyterm`; `words` → segments split on gap > 0.8 s or 12 words, `timestamps_are_synthetic=False`; no words → one synthetic segment), `synthesize_speech_xai()` (`text/voice_id/language/output_format{codec,sample_rate}/speed`, validates ≤15 000 chars, speed 0.7–1.5, codec set) → `(bytes, content_type)`, `list_voices_xai()` (`{"voices":[...]}`). Import-light (httpx + `transcribe_types`), `transport=` injectable.
- `screenscribe/stt_stream.py` (new) — `TranscriptEvent(kind partial|final|ended|error, text, start_ms, end_ms, utterance_id, code)`, `StreamingSttClient` (sync context manager over `websockets` 17.1 asyncio client on a worker thread; `send_pcm`, `finish`, iteration ends on the terminal event; connect/drain timeouts; failures surface as `error` events, never as thread deaths), `LibraxisStreamClient` (session.start frame per `cloud.rs`, 8-byte BE sequence + PCM16LE, stop = `{"type":"flush"}` then `{"type":"end"}` — read from `GatewayCommand::End` in `codescribe/core/asr_session/cloud.rs`, so no assumption needed), `XaiStreamClient` (query `encoding=pcm&sample_rate&interim_results=true[&language]`, raw PCM frames, final = `is_final && speech_final`, `start`/`duration` s → ms), `stream_client_for_endpoint()` by host.
- `screenscribe/config.py` — `XAI_*` constants, `provider_preset("xai", key)` (STT `https://api.x.ai/v1/stt`, LLM/vision `https://api.x.ai/v1/responses`, `stt_model=""`, `llm_model=vision_model="grok-4.6"` — vision model is my choice, plan named only llm), fields `tts_endpoint/tts_api_key/tts_voice/stt_live_endpoint`, env `SCREENSCRIBE_TTS_ENDPOINT/_TTS_API_KEY/_TTS_VOICE/_STT_LIVE_ENDPOINT` (+ file keys), helpers `get_tts_api_key/get_tts_endpoint/get_stt_live_endpoint` (derived from the STT host), `_endpoint_provider` → `"xai"` for `*.x.ai`, `validate()` treats `xai` as known (no libraxis path check; LibraxisAI key aimed at api.x.ai is a hard block), `recognized_provider`, `mismatch_warnings`, `save_default_config` emits the new lines.
- `screenscribe/cli.py` — `screenscribe tts TEXT --out FILE [--voice] [--language] [--speed]` (codec from suffix), `screenscribe transcribe --live [--sample-rate 16000]` (VIDEO now optional; stdin PCM16LE pumped on a thread, `partial:`/`final:` lines, `-o` writes finals, error event → exit 1), `config setup` option 4 = xAI, `config --show` label `xAI`. `pyproject.toml` + `uv.lock`: `websockets>=17.1`. `README.md`/`USAGE.md`: providers paragraph, `tts` section, `--live` row, response_format-by-model note, env table.
- Not done: `--stt-provider` flag (plan table lists it, brief did not; provider comes from config/env).

**Tests added (all mocked, no network; loopback websocket server only):** `tests/test_transcribe_model_capabilities.py` (12), `tests/test_providers_xai.py` (10, `httpx.MockTransport`), `tests/test_stt_stream.py` (8, `websockets.serve` on 127.0.0.1 replaying recorded frames, asserts sent frames/headers/sequence ids), `tests/test_transcribe_xai_dispatch.py` (5), `tests/test_config_xai.py` (9), `tests/test_cli_tts_live.py` (8). `tests/test_transcribe_response_format.py`: the three fallback tests now use an unknown model name (`gpt-transcribe` no longer reaches the 400 path by design).

**Gates:** `make check` green (ruff, mypy strict 64 files, bandit). `make test`: all Lane A tests pass; tree-wide 6 failures in `test_review_app_*` node-harness tests — pre-existing environment issue (pass on a clean `HEAD` worktree at `/tmp/ss-head`; `/Users/maciejgad/vc-workspace/package.json` `"type":"module"` makes Node load `tests/_review_dom.js` as ESM), same finding as Lane B. `make install` done: `~/.local/bin/screenscribe` 0.1.19 now carries `tts`, `transcribe --live` and the model table (version not bumped).

**Verified live vs mocked:** nothing hit api.x.ai or api.libraxis.cloud. Every wire shape is from docs.x.ai (re-fetched 2026-09-10 at `docs.x.ai/developers/model-capabilities/audio/{speech-to-text,text-to-speech}`) and from `cloud.rs`.

**Plan vs docs conflicts found while re-fetching:** (1) docs spell the client finalize frame `{"type":"Finalize"}` (capital F), the plan says `finalize`; Codescribe sends only `audio.done`. I send `Finalize` then `audio.done` — if the server rejects the unknown frame, drop `Finalize` (one line in `XaiStreamClient._end_frames`). (2) docs list extra STT fields not in the plan (`multichannel`, `channels`, `filler_words`, `smart_turn*`, `endpointing`, ws `error` carries `message` not `code`) — not exposed. (3) xAI `transcript.partial` also carries `words[]`; unused.

**Open questions:** should `screenscribe review` on the xAI preset skip `validate_models` for STT (xAI has no STT model; currently `stt_model=""` is simply passed through and unused)? Is `grok-4.6` the right vision model on `/v1/responses` (unverified)? Live-mode drain timeout is 20 s — tune after a real session. Lane B's `_ACCOUNT_HOSTS` could now key off `_endpoint_provider(...) == "xai"`.
